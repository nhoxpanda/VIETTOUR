@using CRM.Core
@using CRMVIETTOUR.Models
@using CRMVIETTOUR.Utilities
@model IEnumerable<tbl_Competitor>

@if (ViewBag.IsAdd != null && ViewBag.IsAdd)
{<a class="btn btn-primary" onclick="createCompetitor()" id="btnAddDoiThu"><i class="fa fa-plus"></i>&nbsp;Thêm</a>}
<br /><br />
<div class="table-scrollable table-custom-format">
    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>#</th>
                <th>Đối thủ</th>
                <th>Ưu điểm</th>
                <th>Nhược điểm</th>
                <th>Nhân viên</th>
                <th>Ngày nhập</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null)
            {
                foreach (var item in Model)
                {
                    <tr class="odd gradeX">
                        <td>
                            @if (ViewBag.IsEdit != null && ViewBag.IsEdit)
                            {<a style="cursor: pointer" onclick="updateCompetitor(@item.Id)"><i class="fa fa-pencil"></i>&nbsp;Sửa</a><br />}
                            @if (ViewBag.IsDelete != null && ViewBag.IsDelete)
                            {<a style="cursor: pointer" onclick="deleteCompetitor(@item.Id)"><i class="fa fa-trash"></i>&nbsp;Xóa</a>}
                        </td>
                        <td>@item.Name</td>
                        <td>@Html.Raw(item.Advantage)</td>
                        <td>@Html.Raw(item.Disadvantage)</td>
                        <td>@item.tbl_Staff.FullName</td>
                        <td>@string.Format("{0:dd-M-yyyy}", item.CreatedDate)</td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

